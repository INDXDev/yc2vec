node_modules
dist

# The dataset is generated by the pipeline and lives in data/public/. It is
# staged here at build time (the Pages workflow does this, and `make site-data`
# does it locally), so committing a second copy would double the repository for
# no benefit.
public/data/
